# Firebase + WebRTC Multi Client

There are 3 Projects, that one avaliable now is : kjhl-vid-conf

Steps :
1. run 'npm install'
2. run project with :

a. localhost : firebase serve --only hosting

b. deploy : firebase deploy --only hosting:kjhl-vid-conf
